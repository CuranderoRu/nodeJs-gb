<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register a new user</title>
    <style type="text/css">
        label,
        button {
            display: block;
            margin: 5px 0;
            position: relative;
        }
        input {
            position: absolute;
            left: 150px;
        }
    </style>
</head>

<body>
    <h1>Вход</h1>
    <form method="POST" action="/web/auth/login/">
        <label>
            Ваш email
            <input type="text" name="email" required="required" value = "{{ email }}" placeholder="Введите ваш email">
        </label><br>
        <label>
            Пароль
            <input type="password" name="password" required="required" placeholder="Введите ваш пароль">
        </label><br>
        <label>
            <input type="checkbox" name="rememberMe"{{#if rememberMe }}checked{{/if}}>Запомнить меня
        </label><br>
        <button type="submit">Войти</button>
    </form>
</body>

</html>

